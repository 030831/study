# 현재 학습 방향과 이어갈 지점

최종 정리: 2026-09-07. 공통 대상: `study` 아래의 현재·향후 프로젝트.
이 문서는 새 대화에서 읽을 짧은 현재 기록이야. [전체 자료 입구](START_HERE.md), [이전 대화 기록](learning/history/learning-direction-before-preparation.md)은 필요할 때만 열어.

## 사용자 목적과 제약

- 컴퓨터공학과 3학년 2학기 재학 중이며, 4학년 졸업 후 Java/Spring 백엔드 신입 취업이 목표다. 정확한 졸업 연월과 희망 회사는 미확인이다.
- Java·JPA·Spring을 사용해 직접 설계·구현하고, 동작과 선택 이유를 설명할 수 있는 실력을 원한다.
- `product`, `spring-commerce-api`는 GPT-5.6 Sol 도움으로 진행했으며 둘 다 미완성이라고 설명했다. 규모가 커질수록 이해하고 혼자 진행하기 어려워졌다.
- 엔티티/DTO/Repository/Service 작성은 반복 경험이 있다. 단순 반복이 취업에 도움이 되는지 의문이며 library만 계속할 계획은 아니다.
- 과거 기록에는 알고리즘 풀이 1,500문제 이상·다이아 티어, JPA 책 재독 경험이 있다. 사용자 과거 설명이며 현재 실력을 직접 검증한 것은 아니다.
- 목적에는 내 코드라는 감각 회복, 기존 쇼핑몰의 독립적인 이해·개선, 백엔드 적성 판단도 포함된다.
- 쉬운 구현만 원하지 않는다. 시간과 어려움을 감수해도 설계 이유를 이해하고 싶다. 그렇다고 기술·클래스 수 증가 자체를 목표로 삼지는 않는다.
- AI 발전에 따른 경쟁력을 걱정한다. 핵심 기능의 자력 구현과, AI 결과를 검증·수정하는 능력을 함께 준비하는 방향에 동의했다.
- 부드러운 반말을 유지하고, Plus 사용량을 고려해 필요한 내용에 집중한다.
- 공통 학습 문서는 `library` 내부가 아닌 `study`에 둔다. 이 대화와 의미 있는 진행 상태를 지속 기록한다.

## 현재 합의한 학습 방식

- CRUD를 계층별로 계속 지시하는 반복에서 벗어나, 한 기능의 처리 흐름·구현·실패·변경을 직접 경험한다.
- 처음에는 행동 요구와 완료 조건을 안내하고, 실제로 막힌 경우 파일/필드/문법을 구체적으로 도와준다. 완성 코드는 대신 쓰지 않는다.
- 사용자는 도메인을 고민하며 틀릴 수 있다. 리뷰에서는 설계 의도와 실제 코드의 어긋남을 먼저 짚는다.
- ‘알겠다’는 반응이나 맞는 코드가 있다는 사실을 독립적인 이해·구현의 증거로 취급하지 않는다.
- 상대 의견을 반박하지 않았다는 이유로 근거가 옳다고 판단하지 않는다. 추천 변경에는 새 사실이나 조건을 설명한다.
- 범위 전체 리뷰를 요청하면 필요한 수정 필드를 모두 알려주되, 파일을 오가는 설명은 구분한다.
- 모르는 import·의존성·API 이름을 암기하게 하지 않는다. 필요한 설명은 질문이나 코드의 실제 막힘에 맞춘다.

## library: 현재 소스와 실행 증거

2026-09-07 소스를 읽은 상태이며 이번 자료 준비 작업에서는 서버·빌드·테스트를 실행하지 않았다.

| 대상 | 확인된 상태 | 아직 확인하지 않은 것 |
|---|---|---|
| 도서 등록/조회 | 컨트롤러·서비스·DTO·저장소·예외 처리 코드 존재 | 최신 소스의 자동 API 테스트 |
| 이전 수동 요청 | 사용자 보고/스크린샷: 등록 201, 빈 제목 400, 단건 200, 미존재 404, 빈/비어 있지 않은 전체 목록 200 | 모든 입력·제약·동시 요청 동작 |
| 엔티티 | Book, Member, Loan, LoanItem 매핑·날짜·목록 초기화 작성 | 저장/재조회 기반 매핑 검증 |
| 테스트 | contextLoads 소스 한 개 | 이번 실행 결과, 대출 기능 테스트 |
| Loan 생성 | 현재는 필드만 있음 | 직전 안내한 생성자 및 날짜 계산 구현 |

서비스 입력은 제목·저자·ISBN 개별 파라미터, 응답은 `ResponseBook(Book)` 생성자로 변환한다. Command는 필수가 아니다. 예전 `BookCreateCommand`와 `toCommand()`가 파일에 남아 있지만 현재 등록 서비스 경로에서 사용하지 않는다. 이 정리는 기능 수정 과제로 자동 추가하지 않는다.

## 대출 모델의 최신 합의

- 회원 한 명은 여러 번 대출한다. 한 번의 대출은 여러 권을 묶는다.
- `LoanItem`은 회원과 대출 사이가 아니라 **대출과 도서 사이**의 책 한 권의 대출 기록이다.
- 관계: 회원 1:N 대출, 대출 1:N 대출도서, 도서 1:N 대출도서.
- `Loan`은 회원, 대출도서 목록, 대출일, 반납예정일을 가진다. 대출 기간은 14일이다.
- `LoanItem`은 대출, 도서, 실제 반납일을 가진다. 부분 반납이 가능하며 미반납이면 실제 반납일은 null이다.
- 대출 전체의 실제 반납일은 중복 저장하지 않는다. 모든 항목이 반납된 경우 마지막 반납일로 계산할 수 있다.
- 대출 기록과 도서 상태 변경은 함께 성공하거나 롤백해야 한다고 사용자가 설명했다. 구현 검증은 아직이다.
- 회원별 5권 한도, 연체 목록, MySQL 전환은 옛 계획의 후속 후보이며 현재 생성자 과제에 섞지 않는다.
- 현재 스키마는 ISBN 유니크인 Book 하나에 대출 상태 하나를 둔다. 같은 ISBN의 실물 여러 권 지원은 미합의 후속 모델 질문이다.

## mappedBy 설명에서 주의할 점

사용자는 ‘주인이 Loan인데 왜 문자열은 member인가’에서 여러 번 혼동했다. 단순히 ‘주인이 누구인지’라고만 설명하지 않는다.

1. 먼저 현재 파일의 목록 타입으로 상대 엔티티를 확인한다.
2. 다른 파일로 넘어간다고 명시한 뒤, 그 엔티티에서 관계를 관리하는 필드/프로퍼티 이름을 확인한다.
3. mappedBy 문자열은 그 이름을 가리키며 현재 쪽은 반대편이다. JoinColumn의 name은 DB 컬럼명이다.

현재 코드는 Member의 목록이 Loan을 대상으로 하고, Loan의 `member` 필드를 가리킨다. Loan과 Book의 목록은 각각 LoanItem의 `loan`, `book` 필드를 가리킨다. 매핑 문자열은 소스에서 맞게 수정된 것을 확인했다. 다른 도메인으로의 이해 전이는 미검증이다.

## 이어갈 지점

직전 사용자 구현 과제는 **Loan 생성자**다.
- 회원과 기준 대출일을 입력받는다.
- 회원·대출일을 보관하고, 상수로 정의한 14일을 더해 반납예정일을 만든다.
- JPA용 protected 기본 생성자를 유지한다.

사용자가 다음에 ‘수정했어’라고 하면 Loan.java부터 읽고 이 과제를 리뷰한다. 준비된 진단 문제를 갑자기 끼워 넣거나 다시 회원 CRUD를 시작하지 않는다.

사용자가 별도 진단을 원하면 [진단 과제](learning/02-diagnostics.md)의 첫 권장 과제를 한 개만 사용한다. 그 결과는 아래에 간단히 추가하고, [학습 순서](learning/03-learning-path.md)를 조정한다. 자료 준비는 끝났어도 사용자 진단은 아직 수행하지 않았다.

## 자료 준비 상태

- 공개 두 저장소의 특정 커밋과 현재 로컬 소스를 근거로 [경험 비교](learning/01-evidence.md)를 작성했다.
- [진단](learning/02-diagnostics.md), 별도 힌트/리뷰 기준, [다른 실험 후보](learning/04-next-experiments.md), [공식자료](learning/05-reference-map.md), [취업 경험 정리](learning/07-career-evidence.md)를 준비했다.
- 이전 학습 기록·기존 협업 지침·이전 SPEC는 `learning/history/`에 보관했다. `conversation.txt`는 원래 위치에 유지한다. 모순되는 과거 확정 문구는 현재 지시가 아니다.
- 기능 코드를 바꾸거나 진단 과제를 대신 풀지는 않았다. 자료 점검 범위는 [QUALITY_CHECK](learning/QUALITY_CHECK.md)에 기록한다.

## 외부 실습자료 선정 기준 수정 — 2026-09-07

사용자는 Spring·JPA로 서버를 구현하는 실습자료를 원하며, 앞서 추가 추천한 세 자료를 제외해달라고 요청했다. 해당 추천과 연결된 안내를 제거했다. reboot-test는 사용자가 제공한 참고자료로만 남긴다. [선정 기준](learning/08-external-practice.md)에 따라 실제 Spring·JPA 구현을 다루는지 확인하며, 순수 Java·SQL 연습이나 Spring 선택이 가능한 일반 서버 명세를 대체 추천으로 넣지 않는다. 현재 새 외부 후보는 선정되지 않았다. 기존 사용자 구현 과제는 이 정리로 변경하지 않는다.

## 다음 갱신

새 진행은 이 문서의 현재 상태와 이어갈 지점을 갱신한다. 필요하면 사건 한 줄을 남기고, 같은 설명을 계속 아래에 쌓지 않는다. 사용자가 직접 밝힌 목적·조건은 유지하고, 오래된 세부 상태는 보관본에서 확인한다.
